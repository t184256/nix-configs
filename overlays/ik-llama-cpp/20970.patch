diff --git a/common/sampling.cpp b/common/sampling.cpp
index 2204d6d..99a4cfc 100644
--- a/common/sampling.cpp
+++ b/common/sampling.cpp
@@ -80,6 +80,18 @@ struct common_sampler * common_sampler_init(const struct llama_model * model, co
 	    result->grammar_root = "root";
     }
     result->grammar = grmr;
+
+    // Resolve thinking boundary tokens for lazy grammar inhibition
+    if (params.grammar_lazy && !params.thinking_start.empty() && !params.thinking_end.empty()) {
+        const llama_vocab * vocab = llama_model_get_vocab(model);
+        const int32_t n = llama_vocab_n_tokens(vocab);
+        for (llama_token id = 0; id < n; id++) {
+            const char * text = llama_vocab_get_text(vocab, id);
+            if (!text) continue;
+            if (params.thinking_start == text) result->thinking_start_token = id;
+            if (params.thinking_end   == text) result->thinking_end_token   = id;
+        }
+    }
     llama_sampling_set_rng_seed(result, params.seed);
     for (const auto& cnstr : params.samplers_sequence)
     {
@@ -534,7 +546,7 @@ static llama_token_data_array llama_sampling_prepare_impl(
     }
 
     // apply grammar checks before sampling logic
-    if (apply_grammar && ctx_sampling->grammar != NULL) {
+    if (apply_grammar && ctx_sampling->grammar != NULL && !ctx_sampling->in_thinking) {
         llama_grammar_sample(ctx_sampling->grammar, ctx_main, &cur_p);
     }
 
@@ -580,7 +592,16 @@ void common_sampler_accept(
     }
     ctx_sampling->prev.push_back(id);
 
-    if (ctx_sampling->grammar != NULL && apply_grammar) {
+    // Track thinking state and inhibit lazy grammar inside thinking blocks
+    if (ctx_sampling->params.grammar_lazy &&
+            ctx_sampling->thinking_start_token != LLAMA_TOKEN_NULL) {
+        if (!ctx_sampling->in_thinking && id == ctx_sampling->thinking_start_token) {
+            ctx_sampling->in_thinking = true;
+        } else if (ctx_sampling->in_thinking && id == ctx_sampling->thinking_end_token) {
+            ctx_sampling->in_thinking = false;
+        }
+    }
+    if (ctx_sampling->grammar != NULL && apply_grammar && !ctx_sampling->in_thinking) {
         llama_grammar_accept_token(ctx_sampling->grammar, ctx_main, id);
     }
     if (ctx_sampling->smpl) {
diff --git a/common/sampling.h b/common/sampling.h
index 7c19c73..efc1c4f 100644
--- a/common/sampling.h
+++ b/common/sampling.h
@@ -96,6 +96,8 @@ typedef struct common_params_sampling {
     std::string grammar;  // optional BNF-like grammar to constrain sampling
     bool                                grammar_lazy = false;
     std::vector<common_grammar_trigger> grammar_triggers; // optional triggers (for lazy grammars)
+    std::string thinking_start; // token text that opens a thinking block (e.g. "<think>")
+    std::string thinking_end;   // token text that closes a thinking block (e.g. "</think>")
     std::set<llama_token>               preserved_tokens;
     // Classifier-Free Guidance
     // https://arxiv.org/abs/2306.17806
@@ -122,6 +124,10 @@ struct common_sampler {
 
     llama_grammar * grammar;
 
+    llama_token thinking_start_token = LLAMA_TOKEN_NULL; // inhibit lazy grammar inside thinking
+    llama_token thinking_end_token   = LLAMA_TOKEN_NULL;
+    bool        in_thinking          = false;
+
     // TODO: replace with ring-buffer
     std::vector<llama_token>      prev;
     std::vector<llama_token_data> cur;
diff --git a/examples/server/server-context.cpp b/examples/server/server-context.cpp
index dcad17e..cf9bd22 100644
--- a/examples/server/server-context.cpp
+++ b/examples/server/server-context.cpp
@@ -1388,6 +1388,9 @@ bool server_context::launch_slot_with_task(server_slot& slot, server_task& task)
         if (slot.ctx_sampling != nullptr) {
             common_sampler_free(slot.ctx_sampling);
         }
+        // Propagate thinking boundaries so grammar is inhibited inside <think> blocks
+        slot.sparams.thinking_start = slot.params.think_tokens.begin;
+        slot.sparams.thinking_end   = slot.params.think_tokens.end;
         slot.ctx_sampling = common_sampler_init(model, slot.sparams);
         if (slot.ctx_sampling == nullptr) {
             // for now, the only error that may happen here is invalid grammar
