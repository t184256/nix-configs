diff --git a/provider/provider.go b/provider/provider.go
index 0028c10..55901ce 100644
--- a/provider/provider.go
+++ b/provider/provider.go
@@ -81,6 +81,7 @@ type Context struct {
 	CursorMarkerCol  int    // 0-indexed byte offset within the stripped line
 	LinesReceived    int    // running count of streamed lines seen by TransformLine
 	SkipLine         bool   // true when TransformLine consumed a marker-only line
+	ExtraMarkers     []string // additional markers to strip from streamed lines
 
 	// Cached editable range for FIM providers. Set by assemblePrompt,
 	// reused by parseCompletion to avoid recomputing.
@@ -132,18 +133,24 @@ func (c *Context) TransformLastLine(line string) string {
 func (c *Context) TransformLine(line string) string {
 	c.SkipLine = false
 	defer func() { c.LinesReceived++ }()
-	if c.CursorMarker == "" {
+	if c.CursorMarker == "" && len(c.ExtraMarkers) == 0 {
 		return line
 	}
-	if !c.CursorMarkerSeen {
+	if !c.CursorMarkerSeen && c.CursorMarker != "" {
 		if idx := strings.Index(line, c.CursorMarker); idx >= 0 {
 			c.CursorMarkerSeen = true
 			c.CursorMarkerLine = c.LinesReceived
 			c.CursorMarkerCol = idx
 		}
 	}
-	stripped := strings.ReplaceAll(line, c.CursorMarker, "")
-	if strings.TrimSpace(stripped) == "" && strings.Contains(line, c.CursorMarker) {
+	stripped := line
+	if c.CursorMarker != "" {
+		stripped = strings.ReplaceAll(stripped, c.CursorMarker, "")
+	}
+	for _, m := range c.ExtraMarkers {
+		stripped = strings.ReplaceAll(stripped, m, "")
+	}
+	if strings.TrimSpace(stripped) == "" && stripped != line {
 		c.SkipLine = true
 	}
 	return stripped
diff --git a/provider/zeta2/zeta2.go b/provider/zeta2/zeta2.go
index 9ae59a1..b0006f9 100644
--- a/provider/zeta2/zeta2.go
+++ b/provider/zeta2/zeta2.go
@@ -62,9 +62,8 @@ const (
 	fimPrefix     = "<[fim-prefix]>"
 	fimMiddle     = "<[fim-middle]>"
 	fileMarker    = "<filename>"
-	currentMarker = "<<<<<<< CURRENT\n"
-	separator     = "=======\n"
-	endMarker     = ">>>>>>> UPDATED\n"
+	startMarker   = "<|marker_1|>"
+	endMarker     = "<|marker_2|>"
 	noEditsMarker = "NO_EDITS"
 	cursorMarker  = "<|user_cursor|>"
 )
@@ -97,19 +96,17 @@ func NewProvider(config *types.ProviderConfig) *provider.Provider {
 			provider.StripRepetition(),
 			parseCompletion,
 		},
-		StopTokens: []string{endMarker, strings.TrimSuffix(endMarker, "\n")},
+		StopTokens: []string{"<|marker_2|>"},
 	}
 }
 
 // armCursorMarkerStripping tells the engine's streaming transform to strip
 // <|user_cursor|> sentinels from every streamed line and record the marker's
-// position. Zeta2's SeedCoder model emits the marker in its output to signal
-// where the cursor should land after the edit is applied; we strip it before
-// the stage builder sees it (to keep it out of the user's buffer) and use
-// the captured position to populate a CursorPredictionTarget in parseCompletion.
+// position. Also strips <|marker_1|> and <|marker_2|> region markers.
 func armCursorMarkerStripping() provider.Preprocessor {
 	return func(p *provider.Provider, ctx *provider.Context) error {
 		ctx.CursorMarker = cursorMarker
+		ctx.ExtraMarkers = []string{"<|marker_1|>", "<|marker_2|>"}
 		return nil
 	}
 }
@@ -143,10 +140,9 @@ func assemblePrompt(p *provider.Provider, ctx *provider.Context, req *types.Comp
 		b.WriteString(fileMarker)
 		b.WriteString(req.FilePath)
 		b.WriteString("\n")
-		b.WriteString(currentMarker)
+		b.WriteString(startMarker)
 		b.WriteString(cursorMarker)
-		b.WriteString("\n")
-		b.WriteString(separator)
+		b.WriteString(endMarker)
 		b.WriteString(fimMiddle)
 		return b.String()
 	}
@@ -225,11 +221,10 @@ func assemblePrompt(p *provider.Provider, ctx *provider.Context, req *types.Comp
 		b.WriteString("\n")
 	}
 
-	b.WriteString(currentMarker)
+	b.WriteString(startMarker)
 	editableText := formatEditableWithCursor(editLines, ctx.CursorLine-editableStart, req.CursorCol)
 	b.WriteString(editableText)
-	ensureTrailingNewline(&b, editableText)
-	b.WriteString(separator)
+	b.WriteString(endMarker)
 	b.WriteString(fimMiddle)
 
 	return b.String()
@@ -494,20 +489,16 @@ func buildEditHistory(history []*types.FileDiffHistory) string {
 func parseCompletion(p *provider.Provider, ctx *provider.Context) (*types.CompletionResponse, bool) {
 	raw := ctx.Result.Text
 
-	// Strip the trailing end marker if the model emitted it (with or without newline).
-	raw = strings.TrimSuffix(raw, endMarker)
-	raw = strings.TrimSuffix(raw, strings.TrimSuffix(endMarker, "\n"))
+	// Strip the trailing end marker if the model emitted it.
+	raw = strings.TrimSuffix(raw, "<|marker_2|>")
 
 	// NO_EDITS sentinel: the model is telling us there's no prediction.
 	if strings.HasPrefix(strings.TrimSpace(raw), noEditsMarker) {
 		return p.EmptyResponse(), true
 	}
 
-	// Strip the cursor marker from the batch-path response. In the streaming
-	// path TransformLine already strips markers before accumulation, so this
-	// is a no-op on clean text. Lines that consist solely of the marker are
-	// removed entirely to avoid phantom trailing empty lines.
-	raw = stripCursorMarker(raw, cursorMarker)
+	// Strip all region and cursor markers from the batch-path response.
+	raw = stripMarkers(raw)
 
 	if raw == "" {
 		return p.EmptyResponse(), true
@@ -534,20 +525,20 @@ func parseCompletion(p *provider.Provider, ctx *provider.Context) (*types.Comple
 	return resp, ok
 }
 
-// stripCursorMarker removes the cursor marker from the response text. Lines
-// that consist solely of the marker (with optional surrounding whitespace)
-// are dropped entirely so they don't produce phantom empty lines in the
-// completion output. Inline markers within content lines are stripped
-// normally.
-func stripCursorMarker(text, marker string) string {
+// stripMarkers removes region and cursor markers from the response text.
+func stripMarkers(text string) string {
+	markers := []string{cursorMarker, "<|marker_1|>", "<|marker_2|>"}
 	lines := strings.Split(text, "\n")
 	out := make([]string, 0, len(lines))
 	for _, line := range lines {
-		if !strings.Contains(line, marker) {
+		stripped := line
+		for _, m := range markers {
+			stripped = strings.ReplaceAll(stripped, m, "")
+		}
+		if stripped == line {
 			out = append(out, line)
 			continue
 		}
-		stripped := strings.ReplaceAll(line, marker, "")
 		if strings.TrimSpace(stripped) == "" {
 			continue // marker-only line — drop it
 		}
diff --git a/provider/zeta2/zeta2_test.go b/provider/zeta2/zeta2_test.go
index a28e94a..4fd811c 100644
--- a/provider/zeta2/zeta2_test.go
+++ b/provider/zeta2/zeta2_test.go
@@ -33,8 +33,8 @@ func TestAssemblePrompt_EmptyBuffer(t *testing.T) {
 	assert.True(t, strings.Contains(prompt, fimMiddle), "contains fim-middle")
 	assert.True(t, strings.HasSuffix(prompt, fimMiddle), "ends with fim-middle")
 	assert.True(t, strings.Contains(prompt, fileMarker+"main.go"), "contains filename marker")
-	assert.True(t, strings.Contains(prompt, currentMarker), "contains CURRENT marker")
-	assert.True(t, strings.Contains(prompt, separator), "contains separator")
+	assert.True(t, strings.Contains(prompt, startMarker), "contains CURRENT marker")
+	assert.True(t, strings.Contains(prompt, endMarker), "contains endMarker")
 	assert.True(t, strings.Contains(prompt, cursorMarker), "contains cursor marker")
 }
 
@@ -70,9 +70,9 @@ func TestAssemblePrompt_StructuralOrder(t *testing.T) {
 	assert.True(t, suffixIdx >= 0 && prefixIdx > suffixIdx && middleIdx > prefixIdx,
 		"tokens in SPM order")
 
-	// Cursor marker lands between CURRENT and separator.
-	currentIdx := strings.Index(prompt, currentMarker)
-	sepIdx := strings.Index(prompt, separator)
+	// Cursor marker lands between CURRENT and endMarker.
+	currentIdx := strings.Index(prompt, startMarker)
+	sepIdx := strings.Index(prompt, endMarker)
 	cursorIdx := strings.Index(prompt, cursorMarker)
 	assert.True(t, currentIdx >= 0 && cursorIdx > currentIdx && cursorIdx < sepIdx,
 		"cursor marker inside CURRENT block")
@@ -795,27 +795,27 @@ func TestAssemblePrompt_ContextOrderingAndCoexistence(t *testing.T) {
 }
 
 func TestStripCursorMarker_InlinePreserved(t *testing.T) {
-	result := stripCursorMarker("    return arr<|user_cursor|>\nother line\n", cursorMarker)
+	result := stripMarkers("    return arr<|user_cursor|>\nother line\n")
 	assert.Equal(t, "    return arr\nother line\n", result, "inline marker stripped, line kept")
 }
 
 func TestStripCursorMarker_MarkerOnlyLineDropped(t *testing.T) {
-	result := stripCursorMarker("  }\n<|user_cursor|>\n", cursorMarker)
+	result := stripMarkers("  }\n<|user_cursor|>\n")
 	assert.Equal(t, "  }\n", result, "marker-only line removed entirely")
 }
 
 func TestStripCursorMarker_MarkerOnlyLineWithWhitespaceDropped(t *testing.T) {
-	result := stripCursorMarker("  }\n  <|user_cursor|>  \n", cursorMarker)
+	result := stripMarkers("  }\n  <|user_cursor|>  \n")
 	assert.Equal(t, "  }\n", result, "whitespace-padded marker line removed")
 }
 
 func TestStripCursorMarker_NoMarker(t *testing.T) {
-	result := stripCursorMarker("hello\nworld\n", cursorMarker)
+	result := stripMarkers("hello\nworld\n")
 	assert.Equal(t, "hello\nworld\n", result, "text without marker unchanged")
 }
 
 func TestStripCursorMarker_MarkerAtEndOfText(t *testing.T) {
-	result := stripCursorMarker("  }\n<|user_cursor|>", cursorMarker)
+	result := stripMarkers("  }\n<|user_cursor|>")
 	assert.Equal(t, "  }", result, "trailing marker-only line removed")
 }
 
