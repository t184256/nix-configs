From b9385fe54a3b88e771f2a9cbaa07d44da1d752a1 Mon Sep 17 00:00:00 2001
From: Sloppy <sloppy@unboiled.info>
Date: Mon, 27 Jul 2026 18:51:23 +0000
Subject: [PATCH 04/22] modes/interactive/interactive-mode: remove borders

---
 .../src/modes/interactive/interactive-mode.ts | 31 +++++++++----------
 1 file changed, 15 insertions(+), 16 deletions(-)

diff --git a/packages/coding-agent/src/modes/interactive/interactive-mode.ts b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
index a4912c7a8..925f7d09b 100644
--- a/packages/coding-agent/src/modes/interactive/interactive-mode.ts
+++ b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
@@ -651,7 +651,7 @@ export class InteractiveMode {
 		if (this.chatContainer.children.length > 0) {
 		// no spacer
 		}
-		this.chatContainer.addChild(new DynamicBorder());
+		// no border
 		if (this.settingsManager.getCollapseChangelog()) {
 			const versionMatch = this.changelogMarkdown.match(/##\s+\[?(\d+\.\d+\.\d+)\]?/);
 			const latestVersion = versionMatch ? versionMatch[1] : this.version;
@@ -665,7 +665,7 @@ export class InteractiveMode {
 			);
 		// no spacer
 		}
-		this.chatContainer.addChild(new DynamicBorder());
+		// no border
 	}
 
 	async init(): Promise<void> {
@@ -3891,21 +3891,21 @@ export class InteractiveMode {
 		const note = release.note?.trim();
 
 		// no spacer
-		this.chatContainer.addChild(new DynamicBorder((text) => theme.fg("warning", text)));
+		// no border
 		this.chatContainer.addChild(
 			new Text(`${theme.bold(theme.fg("warning", "Update Available"))}\n${updateInstruction}`, 0, 0),
 		);
 		if (note) {
 			// no spacer
 			this.chatContainer.addChild(
-				new Markdown(note, 1, 0, this.getMarkdownThemeWithSettings(), {
+				new Markdown(note, 0, 0, this.getMarkdownThemeWithSettings(), {
 					color: (text) => theme.fg("muted", text),
 				}),
 			);
 			// no spacer
 		}
 		this.chatContainer.addChild(new Text(changelogLine, 0, 0));
-		this.chatContainer.addChild(new DynamicBorder((text) => theme.fg("warning", text)));
+		// no border
 		this.ui.requestRender();
 	}
 
@@ -3915,15 +3915,15 @@ export class InteractiveMode {
 		const packageLines = packages.map((pkg) => `- ${pkg}`).join("\n");
 
 		// no spacer
-		this.chatContainer.addChild(new DynamicBorder((text) => theme.fg("warning", text)));
+		// no border
 		this.chatContainer.addChild(
 			new Text(
 				`${theme.bold(theme.fg("warning", "Package Updates Available"))}\n${updateInstruction}\n${theme.fg("muted", "Packages:")}\n${packageLines}`,
-				1,
+				0,
 				0,
 			),
 		);
-		this.chatContainer.addChild(new DynamicBorder((text) => theme.fg("warning", text)));
+		// no border
 		this.ui.requestRender();
 	}
 
@@ -5309,18 +5309,17 @@ export class InteractiveMode {
 		this.resetExtensionUI();
 
 		const reloadBox = new Container();
-		const borderColor = (s: string) => theme.fg("border", s);
-		reloadBox.addChild(new DynamicBorder(borderColor));
+		// no border
 		// no spacer
 		reloadBox.addChild(
 			new Text(
 				theme.fg("muted", "Reloading keybindings, extensions, skills, prompts, themes, and context files..."),
-				1,
+				0,
 				0,
 			),
 		);
 		// no spacer
-		reloadBox.addChild(new DynamicBorder(borderColor));
+		// no border
 
 		const previousEditor = this.editor;
 		this.editorContainer.clear();
@@ -5698,11 +5697,11 @@ export class InteractiveMode {
 				: "No changelog entries found.";
 
 		// no spacer
-		this.chatContainer.addChild(new DynamicBorder());
+		// no border
 		this.chatContainer.addChild(new Text(theme.bold(theme.fg("accent", "What's New")), 0, 0));
 		// no spacer
 		this.chatContainer.addChild(new Markdown(changelogMarkdown, 0, 0, this.getMarkdownThemeWithSettings()));
-		this.chatContainer.addChild(new DynamicBorder());
+		// no border
 		this.ui.requestRender();
 	}
 
@@ -5829,11 +5828,11 @@ export class InteractiveMode {
 		}
 
 		// no spacer
-		this.chatContainer.addChild(new DynamicBorder());
+		// no border
 		this.chatContainer.addChild(new Text(theme.bold(theme.fg("accent", "Keyboard Shortcuts")), 0, 0));
 		// no spacer
 		this.chatContainer.addChild(new Markdown(hotkeys.trim(), 0, 0, this.getMarkdownThemeWithSettings()));
-		this.chatContainer.addChild(new DynamicBorder());
+		// no border
 		this.ui.requestRender();
 	}
 
-- 
2.55.0

