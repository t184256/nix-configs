From 5533e5fc3a9f3feb0b868046654e75eaef77bab2 Mon Sep 17 00:00:00 2001
From: Alexander Sosedkin <monk@unboiled.info>
Date: Mon, 27 Jul 2026 18:44:46 +0000
Subject: [PATCH 02/23] modes/interactive/interactive-mode: remove spacers

---
 .../src/modes/interactive/interactive-mode.ts | 100 ++++++++----------
 1 file changed, 47 insertions(+), 53 deletions(-)

diff --git a/packages/coding-agent/src/modes/interactive/interactive-mode.ts b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
index 6d5e6b88..956d5100 100644
--- a/packages/coding-agent/src/modes/interactive/interactive-mode.ts
+++ b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
@@ -34,7 +34,6 @@ import {
 	Markdown,
 	matchesKey,
 	ProcessTerminal,
-	Spacer,
 	setCapabilityOverrides,
 	setKeybindings,
 	Text,
@@ -466,7 +465,6 @@ export class InteractiveMode {
 	private anthropicSubscriptionWarningShown = false;
 
 	// Status line tracking (for mutating immediately-sequential status updates)
-	private lastStatusSpacer: Spacer | undefined = undefined;
 	private lastStatusText: Text | undefined = undefined;
 	private managedToolStatusStarted = false;
 
@@ -806,7 +804,7 @@ export class InteractiveMode {
 		}
 
 		if (this.chatContainer.children.length > 0) {
-			this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		}
 		this.chatContainer.addChild(new DynamicBorder());
 		if (this.settingsManager.getCollapseChangelog()) {
@@ -816,11 +814,11 @@ export class InteractiveMode {
 			this.chatContainer.addChild(new Text(condensedText, 1, 0));
 		} else {
 			this.chatContainer.addChild(new Text(theme.bold(theme.fg("accent", "What's New")), 1, 0));
-			this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 			this.chatContainer.addChild(
 				new Markdown(this.changelogMarkdown.trim(), 1, 0, this.getMarkdownThemeWithSettings()),
 			);
-			this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		}
 		this.chatContainer.addChild(new DynamicBorder());
 	}
@@ -1011,9 +1009,9 @@ export class InteractiveMode {
 			);
 
 			// Setup UI layout
-			this.headerContainer.addChild(new Spacer(1));
+		// no spacer
 			this.headerContainer.addChild(this.builtInHeader);
-			this.headerContainer.addChild(new Spacer(1));
+			// no spacer
 		} else {
 			// Minimal header when silenced
 			this.builtInHeader = new Text("", 0, 0);
@@ -1726,7 +1724,7 @@ export class InteractiveMode {
 				0,
 			);
 			this.loadedResourcesContainer.addChild(section);
-			this.loadedResourcesContainer.addChild(new Spacer(1));
+			// no spacer
 		};
 
 		const skillsResult = this.session.resourceLoader.getSkills();
@@ -1771,7 +1769,7 @@ export class InteractiveMode {
 				...this.session.resourceLoader.getAgentsFiles().agentsFiles,
 			];
 			if (contextFiles.length > 0) {
-				this.loadedResourcesContainer.addChild(new Spacer(1));
+				// no spacer
 				const contextList = contextFiles
 					.map((f) => theme.fg("dim", `  ${this.formatDisplayPath(f.path)}`))
 					.join("\n");
@@ -1857,7 +1855,7 @@ export class InteractiveMode {
 				this.loadedResourcesContainer.addChild(
 					new Text(`${theme.fg("warning", "[Skill conflicts]")}\n${warningLines}`, 0, 0),
 				);
-				this.loadedResourcesContainer.addChild(new Spacer(1));
+				// no spacer
 			}
 
 			const promptDiagnostics = promptsResult.diagnostics;
@@ -1866,7 +1864,7 @@ export class InteractiveMode {
 				this.loadedResourcesContainer.addChild(
 					new Text(`${theme.fg("warning", "[Prompt conflicts]")}\n${warningLines}`, 0, 0),
 				);
-				this.loadedResourcesContainer.addChild(new Spacer(1));
+				// no spacer
 			}
 
 			const extensionDiagnostics: ResourceDiagnostic[] = [];
@@ -1889,7 +1887,7 @@ export class InteractiveMode {
 				this.loadedResourcesContainer.addChild(
 					new Text(`${theme.fg("warning", "[Extension issues]")}\n${warningLines}`, 0, 0),
 				);
-				this.loadedResourcesContainer.addChild(new Spacer(1));
+				// no spacer
 			}
 
 			const themeDiagnostics = themesResult.diagnostics;
@@ -1898,7 +1896,7 @@ export class InteractiveMode {
 				this.loadedResourcesContainer.addChild(
 					new Text(`${theme.fg("warning", "[Theme conflicts]")}\n${warningLines}`, 0, 0),
 				);
-				this.loadedResourcesContainer.addChild(new Spacer(1));
+				// no spacer
 			}
 		}
 	}
@@ -2316,13 +2314,13 @@ export class InteractiveMode {
 
 		if (widgets.size === 0) {
 			if (spacerWhenEmpty) {
-				container.addChild(new Spacer(1));
+				// no spacer
 			}
 			return;
 		}
 
 		if (leadingSpacer) {
-			container.addChild(new Spacer(1));
+			// no spacer
 		}
 		for (const component of widgets.values()) {
 			container.addChild(component);
@@ -3452,7 +3450,7 @@ export class InteractiveMode {
 					if (event.reason === "manual") {
 						this.showError(event.errorMessage);
 					} else {
-						this.chatContainer.addChild(new Spacer(1));
+						// no spacer
 						this.chatContainer.addChild(new Text(theme.fg("error", event.errorMessage), 1, 0));
 					}
 				}
@@ -3530,13 +3528,12 @@ export class InteractiveMode {
 	/** Show a managed-tool status update in the chat. */
 	private showManagedToolStatus(status: ToolStatus): void {
 		if (!this.managedToolStatusStarted) {
-			this.chatContainer.addChild(new Spacer(1));
+			// no spacer
 			this.managedToolStatusStarted = true;
 		}
 		const message = status.type === "warning" ? `Warning: ${status.message}` : status.message;
 		const color = status.type === "warning" ? "warning" : "dim";
-		this.chatContainer.addChild(new Text(theme.fg(color, message), 1, 0));
-		this.lastStatusSpacer = undefined;
+		this.chatContainer.addChild(new Text(theme.fg(color, message), 0, 0));
 		this.lastStatusText = undefined;
 		this.ui.requestRender();
 	}
@@ -3552,17 +3549,14 @@ export class InteractiveMode {
 		const last = children.length > 0 ? children[children.length - 1] : undefined;
 		const secondLast = children.length > 1 ? children[children.length - 2] : undefined;
 
-		if (last && secondLast && last === this.lastStatusText && secondLast === this.lastStatusSpacer) {
+		if (last && last === this.lastStatusText) {
 			this.lastStatusText.setText(theme.fg("dim", message));
 			this.ui.requestRender();
 			return;
 		}
 
-		const spacer = new Spacer(1);
 		const text = new Text(theme.fg("dim", message), 1, 0);
-		this.chatContainer.addChild(spacer);
 		this.chatContainer.addChild(text);
-		this.lastStatusSpacer = spacer;
 		this.lastStatusText = text;
 		this.ui.requestRender();
 	}
@@ -3620,14 +3614,14 @@ export class InteractiveMode {
 				break;
 			}
 			case "compactionSummary": {
-				this.chatContainer.addChild(new Spacer(1));
+				// no spacer
 				const component = new CompactionSummaryMessageComponent(message, this.getMarkdownThemeWithSettings());
 				component.setExpanded(this.toolOutputExpanded);
 				this.chatContainer.addChild(component);
 				break;
 			}
 			case "branchSummary": {
-				this.chatContainer.addChild(new Spacer(1));
+				// no spacer
 				const component = new BranchSummaryMessageComponent(message, this.getMarkdownThemeWithSettings());
 				component.setExpanded(this.toolOutputExpanded);
 				this.chatContainer.addChild(component);
@@ -3637,7 +3631,7 @@ export class InteractiveMode {
 				const textContent = this.getUserMessageText(message);
 				if (textContent) {
 					if (this.chatContainer.children.length > 0) {
-						this.chatContainer.addChild(new Spacer(1));
+						// no spacer
 					}
 					const skillBlock = parseSkillBlock(textContent);
 					if (skillBlock) {
@@ -3650,7 +3644,7 @@ export class InteractiveMode {
 						this.chatContainer.addChild(component);
 						// Render user message separately if present
 						if (skillBlock.userMessage) {
-							this.chatContainer.addChild(new Spacer(1));
+							// no spacer
 							const userComponent = new UserMessageComponent(
 								skillBlock.userMessage,
 								this.getMarkdownThemeWithSettings(),
@@ -3819,7 +3813,7 @@ export class InteractiveMode {
 		const tokens = usage.input + usage.output + usage.cacheRead + usage.cacheWrite;
 		const cost = usage.cost.total >= 0.01 ? ` (~$${usage.cost.total.toFixed(2)})` : "";
 		const label = notice.kind === "compaction" ? "Compaction" : "Branch summary";
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(
 			new Text(theme.fg("warning", `${label}: ${formatTokens(tokens)} tokens billed${cost}`), 1, 0),
 		);
@@ -3850,7 +3844,7 @@ export class InteractiveMode {
 			label = `Cache miss after ${Math.round(miss.idleMs / 60_000)}m idle`;
 		}
 		const text = theme.fg("warning", `${label}: ${reBilled}`);
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new Text(text, 1, 0));
 	}
 
@@ -3877,7 +3871,7 @@ export class InteractiveMode {
 		}
 
 		if (this.chatContainer.children.length > 0) {
-			this.chatContainer.addChild(new Spacer(1));
+			// no spacer
 		}
 		this.chatContainer.addChild(
 			new Text(
@@ -4256,14 +4250,14 @@ export class InteractiveMode {
 	}
 
 	showError(errorMessage: string): void {
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new Text(theme.fg("error", `Error: ${errorMessage}`), this.outputPad, 0));
 		this.ui.requestRender();
 	}
 
 	showWarning(warningMessage: string): void {
-		this.chatContainer.addChild(new Spacer(1));
-		this.chatContainer.addChild(new Text(theme.fg("warning", `Warning: ${warningMessage}`), 1, 0));
+		// no spacer
+		this.chatContainer.addChild(new Text(theme.fg("warning", `Warning: ${warningMessage}`), this.outputPad, 0));
 		this.ui.requestRender();
 	}
 
@@ -4277,19 +4271,19 @@ export class InteractiveMode {
 		const changelogLine = theme.fg("muted", "Changelog: ") + changelogLink;
 		const note = release.note?.trim();
 
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new DynamicBorder((text) => theme.fg("warning", text)));
 		this.chatContainer.addChild(
 			new Text(`${theme.bold(theme.fg("warning", "Update Available"))}\n${updateInstruction}`, 1, 0),
 		);
 		if (note) {
-			this.chatContainer.addChild(new Spacer(1));
+			// no spacer
 			this.chatContainer.addChild(
 				new Markdown(note, 1, 0, this.getMarkdownThemeWithSettings(), {
 					color: (text) => theme.fg("muted", text),
 				}),
 			);
-			this.chatContainer.addChild(new Spacer(1));
+			// no spacer
 		}
 		this.chatContainer.addChild(new Text(changelogLine, 1, 0));
 		this.chatContainer.addChild(new DynamicBorder((text) => theme.fg("warning", text)));
@@ -4301,7 +4295,7 @@ export class InteractiveMode {
 		const updateInstruction = theme.fg("muted", "Package updates are available. Run ") + action;
 		const packageLines = packages.map((pkg) => `- ${pkg}`).join("\n");
 
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new DynamicBorder((text) => theme.fg("warning", text)));
 		this.chatContainer.addChild(
 			new Text(
@@ -4354,7 +4348,7 @@ export class InteractiveMode {
 		this.pendingMessagesContainer.clear();
 		const { steering: steeringMessages, followUp: followUpMessages } = this.getAllQueuedMessages();
 		if (steeringMessages.length > 0 || followUpMessages.length > 0) {
-			this.pendingMessagesContainer.addChild(new Spacer(1));
+			// no spacer
 			for (const message of steeringMessages) {
 				const text = theme.fg("dim", `Steering: ${message}`);
 				this.pendingMessagesContainer.addChild(new TruncatedText(text, 1, 0));
@@ -5261,7 +5255,7 @@ export class InteractiveMode {
 						this.defaultEditor.onEscape = () => {
 							this.session.abortBranchSummary();
 						};
-						this.chatContainer.addChild(new Spacer(1));
+						// no spacer
 						this.showStatusIndicator(new BranchSummaryStatusIndicator(this.ui));
 						showingSummaryIndicator = true;
 						this.ui.requestRender();
@@ -5937,7 +5931,7 @@ export class InteractiveMode {
 		const reloadBox = new Container();
 		const borderColor = (s: string) => theme.fg("border", s);
 		reloadBox.addChild(new DynamicBorder(borderColor));
-		reloadBox.addChild(new Spacer(1));
+		// no spacer
 		reloadBox.addChild(
 			new Text(
 				theme.fg("muted", "Reloading keybindings, extensions, skills, prompts, themes, and context files..."),
@@ -5945,7 +5939,7 @@ export class InteractiveMode {
 				0,
 			),
 		);
-		reloadBox.addChild(new Spacer(1));
+		// no spacer
 		reloadBox.addChild(new DynamicBorder(borderColor));
 
 		const previousEditor = this.editor;
@@ -6150,7 +6144,7 @@ export class InteractiveMode {
 		if (!name) {
 			const currentName = this.sessionManager.getSessionName();
 			if (currentName) {
-				this.chatContainer.addChild(new Spacer(1));
+				// no spacer
 				this.chatContainer.addChild(new Text(theme.fg("dim", `Session name: ${currentName}`), 1, 0));
 			} else {
 				this.showWarning("Usage: /name <name>");
@@ -6164,7 +6158,7 @@ export class InteractiveMode {
 		if (sessionName !== name) {
 			this.showWarning(`Session name was normalized from ${JSON.stringify(name)} to ${JSON.stringify(sessionName)}`);
 		}
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new Text(theme.fg("dim", `Session name set: ${sessionName ?? name}`), 1, 0));
 		this.ui.requestRender();
 	}
@@ -6227,7 +6221,7 @@ export class InteractiveMode {
 			}
 		}
 
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new Text(info, 1, 0));
 		this.ui.requestRender();
 	}
@@ -6244,10 +6238,10 @@ export class InteractiveMode {
 						.join("\n\n")
 				: "No changelog entries found.";
 
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new DynamicBorder());
 		this.chatContainer.addChild(new Text(theme.bold(theme.fg("accent", "What's New")), 1, 0));
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new Markdown(changelogMarkdown, 1, 1, this.getMarkdownThemeWithSettings()));
 		this.chatContainer.addChild(new DynamicBorder());
 		this.ui.requestRender();
@@ -6375,10 +6369,10 @@ export class InteractiveMode {
 			}
 		}
 
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new DynamicBorder());
 		this.chatContainer.addChild(new Text(theme.bold(theme.fg("accent", "Keyboard Shortcuts")), 1, 0));
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new Markdown(hotkeys.trim(), 1, 1, this.getMarkdownThemeWithSettings()));
 		this.chatContainer.addChild(new DynamicBorder());
 		this.ui.requestRender();
@@ -6391,7 +6385,7 @@ export class InteractiveMode {
 			if (result.cancelled) {
 				return;
 			}
-			this.chatContainer.addChild(new Spacer(1));
+			// no spacer
 			this.chatContainer.addChild(new Text(`${theme.fg("accent", "✓ New session started")}`, 1, 1));
 			this.ui.requestRender();
 		} catch (error: unknown) {
@@ -6425,7 +6419,7 @@ export class InteractiveMode {
 		fs.mkdirSync(path.dirname(debugLogPath), { recursive: true });
 		fs.writeFileSync(debugLogPath, debugData);
 
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(
 			new Text(`${theme.fg("accent", "✓ Debug log written")}\n${theme.fg("muted", debugLogPath)}`, 1, 1),
 		);
@@ -6433,19 +6427,19 @@ export class InteractiveMode {
 	}
 
 	private handleArminSaysHi(): void {
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new ArminComponent(this.ui));
 		this.ui.requestRender();
 	}
 
 	private handleDementedDelves(): void {
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new EarendilAnnouncementComponent());
 		this.ui.requestRender();
 	}
 
 	private handleDaxnuts(): void {
-		this.chatContainer.addChild(new Spacer(1));
+		// no spacer
 		this.chatContainer.addChild(new DaxnutsComponent(this.ui));
 		this.ui.requestRender();
 	}
-- 
2.55.0

